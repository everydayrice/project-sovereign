# Project Sovereign V1

## September 7 cutover recovery

Live reconciliation confirmed main `67235a32b85d94693bee2fab171813e86068a267`, PR #7 at `b2f1817`, PR #8 at `2645b15`, and the unfinished Console branch at `5d3de87`. Neither 0004 nor 0005 was present in production. The existing RICE snapshot was version 3, with one source, one source item, nine candidates and one initialization run; the normalized source/candidate tables were empty.

Main's CI optimization is merged into the PR #7 branch without rewriting history. Recovery fixes include natural-question retrieval, canonical source filtering, excluded-source filtering, tenant-safe upserts and references, task/session insertion order, atomic indexing with source metadata, active-principal checks, safe downloaded-content headers, and a cutover freshness guard. Normalized load compares lightweight ID/revision/timestamp markers against the rollback mirror and returns `normalized_cutover_incomplete` rather than losing legacy writes made between migration and deployment.

Migration authority remains `db/migrations/0004_v1_normalized_runtime_search.sql`; its SQL is unchanged. A fresh prepared migration exists:

- Project: `plain-fog-60342537`; database: `neondb`.
- Production parent: `br-broad-fog-ay47xs6w`.
- Disposable validation branch: `br-super-violet-ay6g5iiq`.
- Prepared migration: `02b2a4a3-07d6-423c-af12-8c97371dab13`.
- Original September 5 validation branch is preserved; its 0005 changes are not part of this migration.

The fresh branch backfilled one source, one item, nine candidates and nine search chunks. Snapshot version 3 and its state digest were preserved. Synthetic repository test tenants exist only on this disposable branch; completing the prepared migration applies the checked-in SQL to the parent, not those fixture rows.

Production migration approval is still required by the owner handoff. Before applying, recheck the RICE snapshot and nonempty collection inventory against this baseline. If production changed, revalidate the migration/backfill. Apply exact 0004 SQL, verify counts and RLS and the untouched snapshot, then merge PR #7 and verify the live application. If the cutover guard fires, preserve both states and reconcile the legacy writes before enabling normalized writes. Never resolve that guard by deleting the mirror. Code rollback uses the prior main revision with the retained, transactionally updated mirror; additive schema changes do not require a destructive rollback.

To rerun repository acceptance, obtain the **disposable branch's** connection string into an untracked environment file. Set `SOVEREIGN_TEST_DATABASE_URL` and `SOVEREIGN_TEST_BRANCH_ID=br-super-violet-ay6g5iiq`, then run `node --env-file=/path/to/private-test.env --test --experimental-test-coverage`. Tests are intentionally locked to the prepared sandbox; never use the production connection string. Coverage without these variables skips real-database tests. The test program exercises natural-language Ask, migration repeatability, RLS under a non-owner role, a second tenant, canonical promotion/revert/history, traffic/checkpoints, concurrent saves, rollback, cross-tenant denial and atomic index failure recovery.

The final September 7 PR #7 validation run passed **46/46 tests with zero skips**, including all four real-Neon tests. Overall line coverage was **87.29%**, with **90.06%** coverage for normalized persistence. The separate orientation branch passed **45/45** local tests. These results are local/disposable-branch verification, not current-head GitHub CI or production acceptance. Publication remains pending because automatic approval review rejected the GitHub push and requested explicit user authorization.

V1 is not released. PR #8, orientation/Console work, the extension contract and Queue Tracker integration, legacy migration/export, source-format expansion, governance, Trust Recovery, browser/mobile acceptance, and production acceptance remain in the V1 contract. None is reclassified as V2.

## Objective

Ship a complete internal alpha of Sovereign core for one real tenant (RICE) without hard-coding RICE into the product model.

V1 must prove that a user can persist intelligence and continuity across AI providers, receive a fast authoritative orientation map, let the authorized agent choose its own context appetite, coordinate concurrent actors before they collide on shared resources, inspect where context came from, and preserve material changes through governed promotion.

## Core platform only

V1 includes the four core modules:

- COMMAND
- INTELLIGENCE
- CONTROL PLANE
- CONTINUITY

It also includes the Protocol, Gateway, Console, Security, Sync, Efficiency telemetry and Extension Host required to operate those modules.

V1 does **not** include Queue Tracker, Idea Tracker, Workforce or Compute as built-in core products. Those are external extensions/add-ons.

## Infrastructure

Project Sovereign owns its own independent infrastructure:

- GitHub repository: `everydayrice/project-sovereign`
- Cloudflare Worker: dedicated Project Sovereign runtime
- Neon project: dedicated Project Sovereign database
- Cloudflare R2 bucket: dedicated Project Sovereign object/source storage
- Neon Auth for authentication

RICE is the first tenant and alpha tester.

## COMMAND V1

- tenant/workspace profile;
- user identity, roles and permissions;
- tenant policy and approval registry;
- authority/source configuration;
- model/agent/tool registry;
- integration registry;
- extension installation, scopes and revocation;
- privacy/retention configuration;
- optional explicit resource/spend ceilings;
- traffic collision policies, lease TTLs and exclusivity rules;
- audit browser;
- tenant branding/personalization, including presenting COMMAND as RICE COMMAND for the RICE tenant.

## INTELLIGENCE V1

- typed canonical records;
- facts, decisions, policies, architecture, projects/entities/domains and relationships;
- source registry and provenance;
- revision/supersession history;
- candidate-to-canonical promotion workflow;
- scoped canonical search/retrieval;
- tenant data export;
- preserved provenance when importing from existing RICE Command.

## CONTINUITY V1

- Session Capsules;
- Task Capsules;
- checkpoints;
- next actions/blockers/waiting state;
- candidate memories;
- Idea primitive/lifecycle objects;
- handoffs/resume packets;
- Traffic Session/Resource Claim resumability where material;
- basic administrative inspection APIs/UI;
- explicit non-canonical semantics unless promoted.

Sovereign core intentionally does not ship a full Queue Tracker or Idea Tracker. RICE Lightning will provide those as alpha extensions using the same public extension contracts available to other publishers.

## CONTROL PLANE V1

Control Plane is orientation-first, traffic-aware and lightweight.

It provides:

- tenant/actor/task/scope orientation;
- relevant entity/project/domain pointers;
- authority/source map;
- privacy/permission/policy boundaries;
- freshness/volatility markers;
- pointers to relevant Intelligence;
- pointers to relevant Continuity/session/task state;
- pointers to relevant live systems and extensions;
- agent/model/tool capability and route options;
- compact orientation packet;
- deterministic packet revision/hash;
- route/orientation trace;
- Traffic Session check-in/check-out;
- Resource Claims with resource, intent, scope, branch/worktree/environment/path where relevant;
- current traffic returned with orientation/resource requests;
- collision classification (`shared`, `caution`, `exclusive`/approval-gated according to Command policy);
- heartbeats/lease TTLs/stale-session recovery;
- traffic re-evaluation when an actor changes or activates resources;
- checkpoint integration and artifact/outcome reporting on checkout.

It does **not** automatically decide the agent's minimum necessary context or hide authorized context behind a platform-selected relevance threshold.

After receiving the orientation packet, the agent/user selects its own context appetite and may retrieve narrowly, broadly or deeply from authorized Intelligence, Continuity and live/source systems.

V1 may expose optional convenience appetite modes such as `lean`, `standard`, `broad`, `deep` or explicit retrieval requests, but the requesting agent/user controls the choice. The platform only enforces Command policy, permissions, privacy boundaries, explicit tenant resource ceilings and hard technical service limits.

Traffic coordination follows [`TRAFFIC-CONTROL.md`](TRAFFIC-CONTROL.md). The V1 objective is collision awareness before broad hard locking: reads and independent branches can coexist; dangerous same-target operations can warn, require approval or reserve exclusivity according to Command policy.

## Protocol V1

Define a versioned vendor-neutral contract covering:

- tenant and actor identity;
- task/scope orientation;
- authority/provenance;
- Intelligence vs Continuity boundaries;
- orientation packets and context retrieval requests;
- agent-directed context appetite;
- Traffic Session check-in before managed-resource work;
- resource/intent declaration before mutation;
- current-traffic inspection and conflict handling;
- heartbeat/checkpoint/re-check when moving between resources;
- checkout/release and stale lease recovery;
- candidate promotion;
- verification/checkpoint/recovery lifecycle;
- permissions;
- extension compatibility;
- auditability.

## Gateway V1

- HTTP/JSON API;
- MCP server over the same internal operations;
- scoped service identities/API credentials;
- provider-neutral schemas;
- read APIs for Command, Intelligence, Continuity and Control Plane;
- orientation endpoint distinct from subsequent Intelligence/Continuity retrieval;
- traffic check-in/current-traffic/claim/checkpoint/release/check-out operations;
- traffic events/webhooks for compatible extensions and agents;
- governed Continuity writes;
- canonical proposal/promotion workflow;
- events/webhooks needed by extensions.

## Extension Host V1

- versioned extension manifest schema;
- manual/private extension installation;
- requested-scope review and approval;
- scoped extension identities;
- event subscriptions;
- external application launch metadata;
- revocation/uninstall;
- audit logging.

No public marketplace is required for V1.

First alpha extension target: `RICE Lightning / Queue Tracker`.

## Console V1

One human-facing shell:

- Home
- Command
- Intelligence
- Control Plane
- Continuity
- Extensions
- Integrations
- Audit / Health

The Console may show basic Continuity object inspectors, but specialized workflow UIs remain extensions.

The Control Plane screen should show the terrain/authority/source map **and live traffic board**: active actors, resources, intent, claim state, last heartbeat/checkpoint, warnings/collisions and stale sessions. It should not pretend that an automatically minimized context bundle is inherently optimal.

## Efficiency V1

V1 should **observe before optimizing**.

Measure:

- orientation packet size;
- context bytes/tokens later requested by the agent;
- depth/breadth chosen by the agent;
- sources/records actually used;
- duplicate context encountered/suppressed;
- unchanged context that could be skipped;
- cache hits;
- route recommendation/selection;
- retries/escalations;
- task corrections/rework where measurable;
- estimated model-input cost where pricing is known;
- request latency where available;
- concurrent Traffic Sessions;
- collision warnings and their outcomes;
- safe concurrent operations;
- stale/abandoned claims;
- conflicts prevented or surfaced before merge/deploy/migration.

Caching, deduplication and known-context delta delivery may be offered when they preserve requested information. V1 must not automatically starve an agent of context merely to reduce token usage.

Once enough real alpha data exists, Sovereign can recommend appetite/routing defaults and later optionally automate them if the tenant enables that behavior through Command. Traffic policy can similarly become more sophisticated based on observed safe/conflicting concurrency patterns.

Efficiency remains a cross-cutting capability, not a module.

## RICE alpha migration

Build an explicit migration/import adapter from `everydayrice/rice-command`.

The migration must:

1. preserve repository/path/commit provenance;
2. distinguish canonical records from working/task/session state;
3. preserve source authority metadata;
4. avoid blindly copying obsolete/superseded records as current;
5. map RICE-specific identifiers through tenant-scoped configuration;
6. allow validation before final promotion/import;
7. retain export/rollback paths;
8. never make existing RICE Command history disappear merely because Sovereign becomes the new operational platform.

The current RICE Control Plane's automatic minimum-sufficient retrieval behavior is migration evidence, not a requirement for Sovereign. The RICE alpha should specifically compare the new orientation-first/agent-directed approach against the old behavior.

## V1 success criteria

V1 is successful when:

1. RICE can authenticate as the first tenant and operate a personalized RICE COMMAND instance.
2. A supported AI agent can connect through MCP or HTTP, receive a fast Sovereign orientation packet and independently request the amount/depth of authorized context it needs without knowing storage internals.
3. The same durable Intelligence is reusable across at least two distinct agent/provider surfaces.
4. Session/Task continuity can be resumed after the original AI session is gone.
5. Working state is never silently treated as canonical truth.
6. A material candidate can be proposed, reviewed/promoted according to Command policy, and read back with provenance/audit history.
7. The Control Plane clearly maps relevant domains, authorities, sources, continuity and available routes without silently rationing the agent's context.
8. Two simultaneous agents can check in against overlapping resources and both receive accurate current traffic before mutation.
9. In the alpha concurrency scenario, an agent planning repo A while another agent is active there receives the warning/state, and traffic is re-evaluated when that planned claim becomes active after work on another resource.
10. Checkpoints and checkouts update/release claims; abandoned sessions become stale through leases rather than remaining permanent locks.
11. Command policy can distinguish safe shared reads/independent branches from cautionary overlap and exclusive critical sections such as same-branch writes, production deploys or migrations.
12. RICE Lightning Queue Tracker can install as an external extension and work through normal scoped APIs/events rather than hidden first-party access.
13. Revoking the Queue extension stops its access without damaging Sovereign core data.
14. Runtime indexes/caches can be rebuilt without losing canonical Intelligence or Continuity source state.
15. Alpha telemetry is sufficient to compare task quality, context usage, rework and concurrency/collision behavior before any automatic minimization or aggressive locking policy is introduced.


## Production release — 2026-09-07

Owner explicitly approved migration 0004 and PR #7 deployment. Prepared migration
`02b2a4a3-07d6-423c-af12-8c97371dab13` was applied to production branch
`br-broad-fog-ay47xs6w`; its temporary validation branch was removed by the migration workflow.
The exact SQL SHA-256 was `575b26ccb8530f50e68ddb8a21f0a9900789dafa11087cfc05716ad52f1e5f55`.

Production verification: 1 source, 1 source item, 9 candidates, 9 search chunks;
source-chunk RLS enabled with tenant checks for both reads and writes. Snapshot
version 3 and digest `6ba91fc2b65e4f1e68413edbdb1df55e` remained unchanged.
PR #7 merged as `dd700f5ac9ff791fd3d1e4fea690193b1c772233` after current-head CI passed.
Live `/health` now reports `normalized_neon`, `postgres_full_text`, and an available
snapshot rollback mirror. Authenticated browser Search/Ask acceptance remains pending
owner sign-in; do not describe it as passed.

PR #8 has been reconciled locally with released main, preserving atomic source
index replacement and all runtime hardening. Migration 0005 has not been applied
to production and still requires fresh preparation, validation and separate approval.
The existing owner account is bound to the existing RICE tenant; creating a new
account must not silently claim or replace that tenant.


## 0005 readiness — 2026-09-07

Authenticated production browser acceptance now passed for sign-in, RICE Console,
source visibility, and Ask: “What is ORBIT TEST's status?” returned ACTIVE with
project-sovereign-test-source.md as its supporting source. The synthetic test
fact remains source evidence and was not promoted into canon. Direct navigation
to the JSON Search endpoint was blocked by the browser client; standalone Search
HTTP acceptance is not claimed. Ask exercised production evidence retrieval.

Fresh prepared 0005 migration: `03eeef21-ae42-498f-8a1c-c858f13cae8a`.
Temporary branch: `br-fancy-voice-aytm0hqp`; production parent:
`br-broad-fog-ay47xs6w`; project: `plain-fog-60342537`; database: `neondb`.
SQL SHA-256: `81b947d537be0ea7a058657a777732e8c1532a33e0084e094dcbbd7b2289832e`.
The migration runner splits statements inside dollar-quoted DO blocks, so the
equivalent four explicit RLS statements replace the loop. Fresh preparation passed.

Validation confirmed four tables and read/write tenant policies, the existing
active RICE principal's Owner binding, unchanged 1 source/1 item/9 candidates/9
chunks and snapshot version 3/digest 6ba91fc2b65e4f1e68413edbdb1df55e.
An Ideas capture/develop revision test succeeded and was rolled back.
Reconciled runtime validation: 47 tests passed, 4 disposable-database tests skipped;
syntax checks passed. Production 0005 remains unapplied and requires owner approval.


## Orientation / machine acceptance — 2026-09-07

Production 0005 was owner-approved and applied. PR #8 merged and deployed as
`166987a57694565e3ab9131d469804bbaf8ce870`. Four new RLS-protected tables, the
RICE Owner binding, authenticated Machine access page and Continuity page were
verified. Unauthenticated HTTP/MCP requests require service credentials.

PR #10 now reconciles with released main and fixes HTTP/MCP Ideas parity by
passing the normalized Idea store through MCP and exposing Idea create/update.
Local syntax checks passed; 49 tests passed, 5 opt-in database tests skipped.

Real machine workflow testing on disposable branch `br-empty-rice-ayt9b4nn`
(project plain-fog-60342537, parent br-broad-fog-ay47xs6w) exercised credential
creation, Ask, scoped tool discovery, denied writes, persisted tasks, orientation,
checkpoints, checkout, cross-actor resume and Ideas. The first run reached explicit
revocation assertions but failed on a WebSocket ErrorEvent during cleanup; the
repeat also failed on a WebSocket ErrorEvent. Neither run is recorded as a fully
passing suite. All disposable acceptance credentials were subsequently revoked.

Automatic approval review blocked the opt-in live production smoke test because
it requires explicit approval for production credential creation/use and live data
reads. Its temporary read-only credential was revoked through narrowly scoped
cleanup. Local connection files were removed. Do not rerun that live test without
explicit owner approval identifying those credential side effects. PR #10 remains
draft pending completion of machine acceptance. No additional schema is required.


## Continued release and guard verification — 2026-09-07

PR #10 merged as `2975f28e6f28886d4553b343dd33f6cfc3158bb1` after current-head CI passed.
The earlier draft/approval notes above are historical: explicit Owner approval was
received and the five read-only production smoke checks passed before that merge.
Those checks verified PR #8's deployed HTTP/MCP release, not PR #10's new orientation.

The next change enforces scoped Trust Recovery automation pauses, preserves original
recovery findings on completion, reports canonical evidence/uncertainty/candidates,
and prevents machine proposals from opting out of human approval. Production browser
governance mutations now consult COMMAND role permissions, and new-tenant Owner
creation is inside the bootstrap transaction. No migration is required.

Ideas inserts and updates now reject foreign-tenant task references in the SQL write
itself; inserts also require an active same-tenant owner. Real Neon verification on
`br-empty-rice-ayt9b4nn` passed all six positive/negative operations. Every fixture
transaction rolled back; the final check found zero fixture tenants.
Local tests: 52 passed, 6 opt-in database tests skipped; syntax checks passed.
Full V1 acceptance, mobile workflows, extension delivery and portability remain open.

## Console workflows — 2026-09-07

PR #11 merged as `44893da00d486ff954b49f6b62e2b021c383f1de` after CI run
34141328438 passed. The next slice adds task create/edit/resume, browser check-in,
checkpoint/checkout, Ideas capture/status, Trust Recovery start/review/complete,
and visible wrapping navigation on mobile. Checkout closes the associated session
capsule while preserving its resume state. Browser Ideas use the same normalized
store as HTTP/MCP. The in-process browser-route workflow passes including foreign
tenant denial; 53 local tests pass with 6 database opt-ins skipped. Syntax checks
pass. Live write-workflow and mobile acceptance are not yet claimed.

Live PR #12 verification found that Cloudflare's bundler inserted `__name` helper
references into the serialized browser function. The controls rendered, but their
script did not initialize; no acceptance task was created. Browser code is now a
literal script, independent of Worker bundling, with a delivered-script regression
test. Forms use POST as their fallback to keep entered text out of URL query strings.

## Extension delivery continuation — 2026-09-07

PR #13 merged as `595d08cbb7a07e020bab08aba589ea350085ab7e`; its browser script
now initializes in production. The labeled Console acceptance task was created
and survived reload. Remaining checkpoint/checkout verification is in progress.

Extension work adds manifest review/install, explicit grants, enable/disable,
revoke/uninstall, audit, normalized event subscriptions/outbox, public HTTP/MCP
poll-and-ack delivery, and credentials bound to the exact installation/grant.
Events contain subject pointers; receivers retrieve content under granted scopes.
Delivery is at least once until acknowledgment; consumers deduplicate by event ID.
Disabling or revoking stops credential authentication and delivery; reinstall creates
a new grant, so a revoked credential's old grant cannot silently become active.

The Queue implementation was located in the private rice-lightning monorepo,
apps/queue/worker/index.ts. It currently reads legacy rice-command projections,
with browser authentication enforced by Lightning. Its Sovereign adapter and
production connection remain unfinished. No private task content was copied into
this public implementation repository.

Extension validation: 56 local tests passed, 7 database opt-ins skipped; syntax checks
passed. Real Neon extension verification passed production-generated bootstrap SQL,
Owner binding, subscription/outbox writes, and credential authentication under active,
disabled and revoked grants. All database fixture transactions rolled back. No
production extension has been installed or connected yet; no migration is needed.
