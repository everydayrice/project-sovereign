name = "project-sovereign"
main = "src/worker.mjs"
compatibility_date = "2026-09-03"

[[r2_buckets]]
binding = "SOVEREIGN_FILES"
bucket_name = "project-sovereign-files"

# Production database/auth runtime bindings are intentionally incomplete until
# the dedicated Neon persistence adapter and Neon Auth JWKS verifier are wired.
# Do not point these names at RICE Lightning or RICE Command infrastructure.
#
# Still required before production activation:
# - HYPERDRIVE or DATABASE_URL binding for the dedicated PROJECT SOVEREIGN Neon project
# - Neon Auth issuer/JWKS configuration consumed by the production authenticator
